class Pig(object):
     def __init__(self, name, pig_x, pig_y, pig_r):
          self.n = name
          self.x = float(pig_x)
          self.y = float(pig_y)
          self.r = float(pig_r)
          self.ispopped = False